# Dependencies
node_modules/

# Build outputs
dist/

# Raw EDGAR data — these are large (250MB+) and are regenerated by npm run refresh-data
# Only public/data/bdc-data.json (the final 1.5MB normalized output) is committed
cache/

# Environment
.env
.env.local
.env.production

# OS
.DS_Store
*.log
